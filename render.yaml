services:
  - type: web
    name: astrovastu-policy
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn policy_server:app --host 0.0.0.0 --port 10000"
